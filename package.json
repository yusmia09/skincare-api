{
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "postinstall": "composer install --no-dev --optimize-autoloader && php artisan storage:link && php artisan config:cache"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.7.2",
        "vite": "^4.0.0"
    }
}
